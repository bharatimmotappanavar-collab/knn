# Iris Flower Classification using K-Nearest Neighbors (KNN)

This project demonstrates the implementation of the **K-Nearest Neighbors (KNN)** algorithm using `scikit-learn` to classify species in the classic **Iris dataset**.
